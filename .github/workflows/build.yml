name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build --no-restore
      
    - name: Test
      run: dotnet test --no-build --collect:"XPlat Code Coverage" --settings coverlet.runsettings

    - name: Restore tools
      run: dotnet tool restore

    - name: Report generator
      run: 'reportgenerator -reporttypes:Html_Dark;Badges -reports:**/TestResults/**/*.xml -targetdir:coverageReport'

    #- name: Upload coverage report artifact
    #  uses: actions/upload-artifact@v3.1.0


      #reportgenerator -reporttypes:'Html_Dark;Badges' -reports:**/TestResults/**/*.xml -targetdir:coverageReport


